---
phase: "00-baseline-evals"
plan: "00-01"
type: "test"
wave: 1
depends_on: []
files_modified:
  - "src/agent/root.zig"
  - "src/gateway.zig"
  - "src/subagent.zig"
  - "src/agent/commands.zig"
  - "src/status.zig"
  - "src/doctor.zig"
autonomous: true
must_haves:
  truths:
    - "All tests must pass: zig build test --summary all"
    - "Release build must succeed: zig build -Doptimize=ReleaseSmall"
  artifacts:
    - "Baseline characterization tests added"
    - "SSE event contract snapshot captured"
---

# Plan 00-01: Baseline Evals and Characterization Tests

## Goal

Create the characterization and eval baseline for the agent core. Every subsequent sprint must not regress these baselines.

## Context

Read before executing:
- `docs/sota-agent-feature-map.md` — competitive parity targets
- `docs/sota-agent-roadmap.md` — full sprint plan
- `src/agent/root.zig` — agent turn loop (6,597 lines, 104 tests)
- `src/gateway.zig` — gateway and SSE (15,599 lines, 262 tests)
- `src/subagent.zig` — subagent system

## Tasks

<task type="auto">
  <name>Characterize the agent turn loop</name>
  <files>src/agent/root.zig</files>
  <action>Add inline tests that characterize the current turn loop behavior:
  1. Test that a direct text response completes without tool calls
  2. Test that tool dispatch respects max_tool_iterations (25 default)
  3. Test that tool iteration exhaustion produces a text summary
  4. Test that the reflection message is appended after tool execution
  5. Test that context token calculations are correct for known models
  Read the existing test patterns in the file and follow the same style.</action>
  <verify>zig build test --summary all | grep -E "pass|fail"</verify>
  <done>5+ new characterization tests pass for agent turn loop</done>
</task>

<task type="auto">
  <name>Characterize SSE event contract</name>
  <files>src/gateway.zig</files>
  <action>Add inline tests that snapshot the current SSE event grammar:
  1. Test that chat stream emits status/progress/token/done event types
  2. Test that reasoning_summary events are emitted when reasoning is on
  3. Test that the done event is always terminal
  4. Test rate limiter behavior (sliding window, sweep interval)
  5. Test idempotency store deduplication
  Read existing gateway tests (262 tests) and follow patterns.</action>
  <verify>zig build test --summary all | grep -E "pass|fail"</verify>
  <done>5+ new SSE contract characterization tests pass</done>
</task>

<task type="auto">
  <name>Characterize subagent lifecycle</name>
  <files>src/subagent.zig</files>
  <action>Add inline tests that characterize subagent behavior:
  1. Test that SubagentManager respects max_concurrent (4)
  2. Test that task state tracks queued/running/completed
  3. Test that tool restrictions are enforced (no message, spawn, delegate)
  4. Test that completion results are routed via event bus
  Read existing patterns and follow style.</action>
  <verify>zig build test --summary all | grep -E "pass|fail"</verify>
  <done>4+ new subagent characterization tests pass</done>
</task>

<task type="auto">
  <name>Characterize command surface</name>
  <files>src/agent/commands.zig</files>
  <action>Add inline tests that characterize operator command behavior:
  1. Test that /status produces expected output sections
  2. Test that /context returns context report data
  3. Test that /model switch updates agent config
  4. Test that unknown commands return helpful error
  Read existing patterns (commands.zig has tests) and follow style.</action>
  <verify>zig build test --summary all | grep -E "pass|fail"</verify>
  <done>4+ new command characterization tests pass</done>
</task>

<task type="auto">
  <name>Verify release build</name>
  <files></files>
  <action>Run the full release build to confirm no regressions:
  1. zig build test --summary all
  2. zig build -Doptimize=ReleaseSmall
  3. zig build -Dengines=base,sqlite,postgres (if postgres available)
  Report any failures.</action>
  <verify>zig build -Doptimize=ReleaseSmall 2>&1; echo "EXIT: $?"</verify>
  <done>All builds pass with exit code 0</done>
</task>

## Acceptance Criteria

1. 18+ new characterization tests added and passing
2. SSE event contract is snapshotted (any future change that breaks these tests is a regression)
3. Subagent lifecycle is characterized
4. Command surface is characterized
5. Full build passes (test + release)
6. No existing tests broken
