---
phase: "00-baseline-evals"
plan: "00-02"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "docs/openapi-v1.yaml"
  - "docs/online-agent-contract.md"
  - "docs/agent-lifecycle-spec.md"
autonomous: true
must_haves:
  truths:
    - "No runtime code changes"
    - "Documents are consistent with current gateway behavior"
  artifacts:
    - "online-agent-contract.md created"
    - "agent-lifecycle-spec.md created"
---

# Plan 00-02: Online Agent Contract Documentation

## Goal

Document the online agent control contract before implementation churn. This gives Phase 2 (run events, tasks) a target vocabulary.

## Context

Read before executing:
- `docs/openapi-v1.yaml` — current API contract
- `src/gateway.zig` — current endpoint handlers and SSE events
- `docs/sota-agent-feature-map.md` — F2 (run events) and F3 (task ledger) specs

## Tasks

<task type="auto">
  <name>Document target run-event vocabulary</name>
  <files>docs/online-agent-contract.md</files>
  <action>Create docs/online-agent-contract.md defining the target SSE event grammar:
  1. List current events: status, progress, token, done, reasoning_summary
  2. Define target events: ready, reply_start, progress, tool_start, tool_result, approval_required, task_update, subagent_completion, reasoning_summary, token, done
  3. For each event: type name, payload schema, when emitted, terminal flag
  4. Define replay contract: how a client reconstructs the run from events
  5. Define backward compatibility rules: current clients must not break</action>
  <verify>test -f docs/online-agent-contract.md && echo "EXISTS"</verify>
  <done>Run-event vocabulary documented with schemas and compatibility rules</done>
</task>

<task type="auto">
  <name>Document target task vocabulary</name>
  <files>docs/agent-lifecycle-spec.md</files>
  <action>Create docs/agent-lifecycle-spec.md defining:
  1. Task states: queued, running, succeeded, failed, timed_out, cancelled, lost
  2. Task record fields: id, label, session_key, origin, status, result, created_at, updated_at
  3. Delivery modes: direct (to channel), session-queued, replay
  4. Lifecycle transitions and what triggers each
  5. Relationship between tasks and subagents
  6. Relationship between tasks and cron jobs</action>
  <verify>test -f docs/agent-lifecycle-spec.md && echo "EXISTS"</verify>
  <done>Task lifecycle vocabulary documented</done>
</task>

<task type="auto">
  <name>Document target session-control API semantics</name>
  <files>docs/openapi-v1.yaml</files>
  <action>Add comments/documentation to openapi-v1.yaml noting planned additions:
  1. Mark existing endpoints as stable
  2. Add TODO comments for planned endpoints: session resume, compact, export, task list/get/stop
  3. Note planned SSE event extensions
  Do NOT change actual API behavior — documentation only.</action>
  <verify>grep -c "TODO" docs/openapi-v1.yaml</verify>
  <done>OpenAPI spec annotated with planned extensions</done>
</task>

## Acceptance Criteria

1. online-agent-contract.md exists and defines all target run events
2. agent-lifecycle-spec.md exists and defines task lifecycle
3. openapi-v1.yaml annotated with planned extensions
4. No runtime code changes
5. Documents are consistent with current gateway.zig behavior
