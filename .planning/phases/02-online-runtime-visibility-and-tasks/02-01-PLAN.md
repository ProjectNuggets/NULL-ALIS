---
phase: 02-online-runtime-visibility-and-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/run_event_types.zig
  - src/agent/root.zig
autonomous: true
requirements:
  - REQ-004

must_haves:
  truths:
    - "RunEventType enum covers all 8 event kinds from REQ-004: ready, reply_start, progress, tool_start, tool_result, approval_required, task_update, done"
    - "RunEvent struct carries event type, timestamp, and a tagged union payload for each event kind"
    - "RunEvent can be converted to a JSON-serializable SSE payload without allocation beyond the output buffer"
  artifacts:
    - path: "src/agent/run_event_types.zig"
      provides: "RunEventType enum, RunEvent struct, per-event payload structs"
      contains: "pub const RunEventType"
    - path: "src/agent/root.zig"
      provides: "run_event_types module re-export"
      contains: "pub const run_event_types"
  key_links:
    - from: "src/agent/run_event_types.zig"
      to: "src/observability.zig"
      via: "import for NarrationFrameType"
      pattern: "observability"tags: [prose, prose/planning]
---

<objective>
Define the structured run-event type system that all online clients consume.

Purpose: REQ-004 requires a structured run-event stream with 8 event kinds (ready, reply_start, progress, tool_start, tool_result, approval_required, task_update, done). This plan creates the type definitions — the contract that SSE transport (Plan 02), task delivery (Plan 04), and cost reporting (Plan 06) all emit through.

Output: src/agent/run_event_types.zig with RunEventType enum, RunEvent struct, payload types, and a toSseFrame serializer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/observability.zig
@src/agent/root.zig
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run_event_types.zig with RunEventType enum, RunEvent struct, payload types, and toSseFrame serializer</name>
  <files>src/agent/run_event_types.zig</files>
  <read_first>src/observability.zig, src/agent/root.zig, src/gateway.zig (lines 7795-7880 for SSE frame format)</read_first>
  <behavior>
    - Test 1: RunEventType enum has exactly 8 variants: ready, reply_start, progress, tool_start, tool_result, approval_required, task_update, done
    - Test 2: RunEventType.toSlice returns correct SSE event name strings (e.g., .ready -> "ready", .tool_start -> "tool_start")
    - Test 3: RunEvent.ready payload contains session_key field
    - Test 4: RunEvent.progress payload contains phase, state, label fields (matching existing sseProgressFrame contract)
    - Test 5: RunEvent.tool_start payload contains tool name string
    - Test 6: RunEvent.tool_result payload contains tool name, success bool, duration_ms
    - Test 7: RunEvent.task_update payload contains task_id, status, description
    - Test 8: RunEvent.done payload contains optional session_id, optional message_id
    - Test 9: toSseFrame produces "event: {type}\ndata: {json}\n\n" format
    - Test 10: toSseFrame for ready event contains "session_key" in JSON output
  </behavior>
  <action>
Create src/agent/run_event_types.zig with:

1. **RunEventType** enum with 8 variants matching REQ-004:
   - ready, reply_start, progress, tool_start, tool_result, approval_required, task_update, done
   - Add `pub fn toSlice(self: RunEventType) []const u8` returning the SSE event name

2. **Payload structs** for each event type (all fields are `[]const u8` or primitives, no heap allocation):
   - ReadyPayload: { session_key: []const u8 }
   - ReplyStartPayload: { stream_kind: []const u8, delivery_mode: []const u8, live: bool }
   - ProgressPayload: { phase: []const u8, state: []const u8, label: []const u8, tool: ?[]const u8, iteration: ?u32, duration_ms: ?u64 }
   - ToolStartPayload: { tool: []const u8 }
   - ToolResultPayload: { tool: []const u8, success: bool, duration_ms: u64, output_preview: ?[]const u8 }
   - ApprovalRequiredPayload: { tool: []const u8, reason: []const u8, risk_level: []const u8 }
   - TaskUpdatePayload: { task_id: []const u8, status: []const u8, description: ?[]const u8, progress_pct: ?u8 }
   - DonePayload: { session_id: ?[]const u8, message_id: ?i64, usage_tokens: ?u64, cost_usd: ?f64 }

3. **RunEvent** tagged union:
   ```zig
   pub const RunEvent = union(enum) {
       ready: ReadyPayload,
       reply_start: ReplyStartPayload,
       progress: ProgressPayload,
       tool_start: ToolStartPayload,
       tool_result: ToolResultPayload,
       approval_required: ApprovalRequiredPayload,
       task_update: TaskUpdatePayload,
       done: DonePayload,
   };
   ```

4. **toSseFrame** function:
   `pub fn toSseFrame(allocator: std.mem.Allocator, event: RunEvent) ![]u8`
   - Writes "event: {event_type}\ndata: {json_payload}\n\n"
   - Uses ArrayListUnmanaged(u8) writer pattern matching gateway.zig's sseProgressFrame
   - Manually writes JSON fields (no std.json.stringify) for zero-alloc payload serialization
   - Use a helper `jsonEscapeInto` copied from gateway.zig or implemented inline (escape `"`, `\`, control chars)

5. **eventType** accessor:
   `pub fn eventType(event: RunEvent) RunEventType` — returns the active tag as RunEventType

Write all tests inline at the bottom of the file using `test "..."` blocks.
  </action>
  <verify>
    <automated>cd /Users/nova/Desktop/nullalis/.claude/worktrees/nifty-rhodes && zig build test --summary all 2>&1 | tail -5</automated>
  </verify>
  <done>RunEventType has 8 variants, RunEvent tagged union compiles, toSseFrame produces valid SSE frames, all inline tests pass, zig build -Doptimize=ReleaseSmall succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Wire run_event_types module into agent root</name>
  <files>src/agent/root.zig</files>
  <read_first>src/agent/root.zig (lines 38-53 for existing module re-exports)</read_first>
  <action>
Add the run_event_types module re-export to src/agent/root.zig:

1. Add `pub const run_event_types = @import("run_event_types.zig");` after the existing `learning` import (line ~50)

2. Add a reexport test at the bottom of root.zig alongside existing reexport tests:
   ```zig
   test "run_event_types reexport" {
       _ = run_event_types.RunEventType;
       _ = run_event_types.RunEvent;
       _ = run_event_types.toSseFrame;
   }
   ```

3. Ensure the comptime anonymous test block (if one exists) includes `_ = @import("run_event_types.zig");`
  </action>
  <verify>
    <automated>cd /Users/nova/Desktop/nullalis/.claude/worktrees/nifty-rhodes && zig build test --summary all 2>&1 | tail -5</automated>
  </verify>
  <done>run_event_types module exported from agent root, reexport test passes, full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunEvent -> SSE wire | RunEvent payloads will be serialized to JSON for SSE transport; fields must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | toSseFrame | mitigate | output_preview in ToolResultPayload must be truncated to prevent leaking full tool output; truncation to 256 chars enforced in toSseFrame |
| T-02-02 | T (Tampering) | RunEvent payloads | accept | RunEvent is constructed server-side only; no user input directly populates fields. Escaping handles special chars in tool names. |
</threat_model>

<verification>
- grep -c "pub const RunEventType" src/agent/run_event_types.zig returns 1
- grep -c "ready" src/agent/run_event_types.zig returns >= 3 (enum variant + payload + test)
- grep -c "pub const run_event_types" src/agent/root.zig returns 1
- zig build test --summary all passes
- zig build -Doptimize=ReleaseSmall succeeds
</verification>

<success_criteria>
RunEventType enum with 8 variants, RunEvent tagged union with per-event payloads, toSseFrame serializer producing valid SSE frames, module re-exported from agent root, all tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-online-runtime-visibility-and-tasks/02-01-SUMMARY.md`
</output>
