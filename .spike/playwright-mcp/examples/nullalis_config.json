{
  "_comment": "Drop the `mcp_servers.browser` block into your ~/.nullalis/config.json (or merge with an existing `mcp_servers` map). Then any nullalis tool-loop iteration where the model issues a tools/call against one of these names is dispatched to this server.",
  "mcp_servers": {
    "browser": {
      "command": "node",
      "args": [
        "/absolute/path/to/nullalis/.spike/playwright-mcp/dist/server.js"
      ],
      "env": {
        "PLAYWRIGHT_HEADLESS": "true",
        "PLAYWRIGHT_MCP_ALLOWLIST": "",
        "PLAYWRIGHT_MCP_ALLOW_EVAL": "0"
      }
    }
  }
}
