apiVersion: v1
kind: Service
metadata:
  name: nullclaw
  namespace: zaki-agent-staging
  labels:
    app.kubernetes.io/name: nullclaw
    app.kubernetes.io/part-of: zaki-agent
spec:
  selector:
    app.kubernetes.io/name: nullclaw
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
