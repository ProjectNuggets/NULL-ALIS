---
tags: [prose, prose/docs, prose/audit]
authored: 2026-05-20
purpose: Post-sprint activation audit — what each agent's work activates, what doesn't, what's latent
binds_to: AGENTS.md §14.10 (audit discipline) + docs/ROADMAP.md v1.14.18-B + v1.14.19
---

# 2026-05-20 — v1.14.14 + v1.14.18-A In-Flight + v1.14.14.1 In-Flight Activation Audit

This is the inaugural execution of the audit discipline codified in AGENTS.md §14.10.
It exists because the v1.14.14 sprint (Agent G) shipped code-complete but largely
behaviorally inert; only Nova's prompt about WM importance saturation surfaced the
activatable follow-up work. Without that prompt, ~40% of in-flight agent value would
have remained latent.

The audit happens BEFORE the next bench run.

## Agent A — V8 sandbox triple-gate (commit 4dec8711)

| Aspect | Verdict |
|---|---|
| Tier | 🟢 BEHAVIORAL (security-context-only) |
| What activates | Triple-gate (`fail_open_on_dev=true` AND `NULLALIS_ALLOW_UNSANDBOXED_DEV=1` AND backend=none) closes the unsandboxed-shell hole |
| Bench-measurable effect | ZERO. Bench gateway has Docker backend. V8 path never fires. |
| Cascade enables | Operator-confidence in prod deploys with misconfigured sandbox; alarmable via log.err |
| Latent value left unclaimed | None. Security work is islanded by design. |

## Agent D — τ-bench harness + B2 latency columns (commits 3025273f + 015dc461 + 8e638901)

| Aspect | Verdict |
|---|---|
| Tier | 🟡 VISIBILITY |
| What activates | Ability to measure τ-bench Airline (iter22 baseline 0.10, iter23 baseline 0.35 on Together llama 70B); p50/p95 TTFT columns in results.tsv |
| Bench-measurable effect | The bench data itself; agent code untouched |
| Cascade enables | Future τ-bench iterations attributable to specific changes |
| Latent value left unclaimed | **No automated regression detection** — τ-bench numbers drift, nobody alarmed. Closes via v1.14.19 SC5 (continuous canary). |

## Agent E v1.14.13 — Cluster B/C false-confidence + B1 + F-A2 + IDENTITY-ORPHAN + B13

| Finding | Tier | Bench-measurable | Cascade |
|---|---|---|---|
| B1 (skillforge→skills.zig docs) | ⚪ HYGIENE | none | — |
| F-A2 strip | 🟢 BEHAVIORAL (small) | -250 bytes from prompt; model was ignoring the directive (0/145 calls) so zero tool-selection change | Replaced by goal-loop reflection path (v1.14.18-A Finding 3) |
| HND-READY rewire | ⚪ HYGIENE | gateway endpoint dedup; not agent-facing | — |
| EMPTY_TURN_PLACEHOLDER strip | ⚪ HYGIENE | dead constant removed | — |
| BROWSER-HONESTY | 🟢 BEHAVIORAL (small) | Schema 6→2 actions; "unknown action" vs "needs CDP"; tighter rejection | Benches rarely exercise browser tool; lift not directly measurable |
| BIRTHDAY-DOC | ⚪ HYGIENE | docstring fix | — |
| IDENTITY-ORPHAN | ⚪ HYGIENE | header comment added; archaeology preserved | Substrate parked for V-infinity persona pillar |
| B13 MaxRSS triage | ⚪ HYGIENE | documented deferral | — |

**Activation balance:** 2 of 8 findings BEHAVIORAL (small), 6 HYGIENE. The honest read: v1.14.13 Agent E shipped mostly clean-up + one prompt-shrink. The MED-tier behavior payoff comes via Agent E v1.14.18-A's mode unification + structured tool descriptions + goal-loop.

## Agent F v1.14.13 — SCHEMA-WIRE + TASK-PLANNER + NARRATION

| Finding | Tier | What activates | Latent value if any |
|---|---|---|---|
| SCHEMA-WIRE | 🟢 BEHAVIORAL | Parallel tool calls now serialize correctly through `cleanForProvider`. Verified in V-inf with `tool_calls=2`, `tool_calls=3` parallel events. | Schema cleaner unlocks Anthropic two-block cache (deferred D17) |
| TASK-PLANNER-WIRE | 🟡 VISIBILITY | `emitStepProgress/Done/Complete` SSE events fire | **Plan structure NEVER re-read by agent** — read-back via goal-loop reflection (v1.14.18-A + v1.14.18-B G3) closes the loop |
| NARRATION-WIRE | 🟡 VISIBILITY | `narration type=thinking` events to channel UIs | **Agent doesn't see own narration in next iteration** — closes via v1.14.18-B G3 narration-as-context |

**Activation balance:** 1 of 3 truly BEHAVIORAL. SCHEMA-WIRE is the genuine lift; both planner + narration wires are observability-only at HEAD, missing the agent-side read-back loop.

## Agent G v1.14.14 — ContextEngine migration (commits faf1ba56 → ad8c159a + Phase 5 + self-review pass)

| Phase | Tier | What activates |
|---|---|---|
| 1 INGEST | ⚪ HYGIENE | Code organized; behavior parity by design |
| 2 ASSEMBLE | ⚪ HYGIENE + 🟡 VISIBILITY (`stable_prefix_hash`) | prefix-hash diagnostic exists |
| 3 COMPACT | ⚪ HYGIENE | 8 auto + 3 force callsites routed; behavior parity |
| 4 AFTERTURN | 🟡 VISIBILITY | stability.jsonl emission (env-gated, default off in prod) |
| 5 (added by Nova directive) | 🟡 VISIBILITY (CI gate) | jq drift-detection block in `.spike/run.sh` fails bench on prefix-hash drift |
| Self-review pass | 🟢 BEHAVIORAL (concurrency bug fix) | `writeStabilityJsonl` lock=exclusive fix prevents JSONL corruption under concurrent writes |

**Bench-measurable effect of v1.14.14 (before merge):** LoCoMo 40/50 = 0.800 (+0.020 vs v1.14.13 baseline 0.78). Sample 8 lifted +0.200. Within stochasticity but consistent — possibly the ContextEngine.assemble path is slightly more cache-friendly than inline. Either way: **not a regression, possible micro-lift**.

**Activation balance:** 1 of 6 phases BEHAVIORAL (self-review concurrency fix). 5 of 6 are HYGIENE / VISIBILITY. The migration's value is the substrate it provides for future behavioral changes, not behavioral lift itself.

## Agent E v1.14.18-A Finding 2 — TOOL-DESC-AUDIT (in-flight, complete; 6 commits)

| Aspect | Verdict |
|---|---|
| Tier | 🟢 BEHAVIORAL |
| What activates | 52 agent-facing tools migrated from freeform descriptions to `ToolDescription` struct. Comptime lint enforces Rules 1-4 (Rule 5 deferred). 340+ sibling cross-references validated. Self-review caught 3 real bugs (placeholder use_when entries, sibling reference inversion, registry mismatches). |
| Bench-measurable effect | Tool selection signal density improved across all tools — expected to lift τ-bench action-selection. Quick smoke after merge will quantify. |
| Cascade enables | (a) Skill differentiation in v1.14.19 SC1 — instead of "generic_multi_tool" trace, can map to specific tool sequences via structured catalog; (b) Tool description Rule 5 (rendered ≥200 chars) re-enablable as a tightening pass; (c) Cross-tool xref lint catches stale references when any tool is renamed |
| Latent value left unclaimed | Catalog generation (Phase D+) deferred — single-file audit surface from struct source-of-truth not yet generated. Quick follow-up. |

## Agent G v1.14.14.1 — 5 findings (4 of 5 in flight, F1 paused for ack)

| Finding | Tier | What activates |
|---|---|---|
| F2 PREFIX-TAIL-SURFACE (a9ca41f6) | 🟡 VISIBILITY | tail_hash + tail_bytes through AssembleResult + JSONL; Phase 5 jq covers both halves |
| F3 COMPACT-MS-AGGREGATE option b (fda8499b) | ⚪ HYGIENE | rename to honest `compact_ms_main_site_only`; option a deferred to v1.14.14.2 |
| F4 COMPACT-SENTINEL-RESOLVE (4e8cb7ee) | ⚪ HYGIENE | fields removed; `agent.last_turn_context` is source of truth |
| F5 STABILITY-JSONL-CANARY-RUNBOOK (398ac622) | ⚪ HYGIENE | 237-line ops runbook |
| F1 WM-IMPORTANCE-CALIBRATION option b (pending ack) | 🟢 BEHAVIORAL (small) | drops importance from composite eviction priority — tie-breaks now via slot_type_weight; per-source calibration deferred to v1.14.18-B G6 sub-block |

**Activation balance:** 1 of 5 BEHAVIORAL (small), 1 VISIBILITY, 3 HYGIENE. The real WM behavioral lift is the per-source calibration (option a) deferred explicitly.

## 19 latent-value gaps (G1-G19)

Combined across all in-flight + just-shipped work, the unclaimed value falls into these buckets:

### Closed by v1.14.18-A (Agent E in flight)
- **G1 Procedural memory empty** — capture-gate fix (Finding 3)
- **G4 task_planner observability-only** — goal-loop reads plan back (Finding 3)
- **G11 brain_graph 0/145** — reflection escalation per two-stage recall pattern (Finding 3 prompt)

### Closed by v1.14.14.1 (Agent G in flight)
- **G2 WM importance saturated** — option b drops from composite (Finding 1)
- **G12 prefix.tail dormant** — surfaced (Finding 2)
- **G13 compact_ms partial** — rename (Finding 3)
- **G14 sentinel** — removed (Finding 4)
- **G15 stability JSONL not in prod** — runbook landed; deploy is operator (Finding 5)

### Targeted by v1.14.18-B (locked, post-A merge)
- **G3 NarrationObserver one-way** — agent sees own narration in volatile prompt
- **G5 Reflection-trail not stored** — Agent E reflections write to skill_executions.assumptions_made_json
- **G7 Agent doesn't see bench data** — `<known_weakness>` block from results.tsv
- **G16 Cross-session continuity** — promote active_goal + decision to durable_facts
- **G19 daemon-prompt-honesty** — implement or strip nightly-summaries prompt line

### Targeted by v1.14.19 sleep cycle (proposed, locked)
- **G6 Memory layers don't promote** — explicit promotion graph (initial WM→durable in v1.14.18-B G16, expanded in v1.14.19)
- **G17 community_pipeline no scheduler** — wired to daemon nightly tick (SC4)
- **G18 hygiene does no skill consolidation** — extended with consolidation pass (SC1)

### Deferred to later sprints
- **G8 Sub-agents silo state** — v1.16.0 / v1.17.0
- **G9 No continuous τ-bench canary** — folds into v1.14.19 sleep cycle observability (SC5 covers it)
- **G10 No A/B harness** — explicitly dropped; sleep cycle replaces this as the iteration mechanism

## SOTA gap matrix snapshot

| SOTA capability | Reference | nullalis state |
|---|---|---|
| Self-curated memory tiers | MemGPT/Letta | ⚠ Tools exist; agent rarely uses them. v1.17.0+ |
| Reflection storage for compounded learning | Reflexion (Shinn 2023) | 🏃 v1.14.18-A Finding 3 builds reflection; v1.14.18-B G5 stores it |
| Skill library that grows over time | Voyager (Wang 2023) | 🏃 Schema exists (skill_executions); empty until v1.14.18-A capture-gate fix; consolidated in v1.14.19 SC1 |
| Continuous self-evaluation | DSPy | 🏃 Sleep cycle observability (v1.14.19 SC5) gets us partial |
| Plan-decompose-verify | ReAct | 🏃 v1.14.18-A goal-loop |
| KV-cache drift monitoring | Anthropic 2024 + vLLM | ✅ Agent G v1.14.14 + v1.14.14.1 both halves |
| Tool synthesis | CodeAct (Wang 2024) | ✗ V-infinity territory |

**Net trajectory:** by v1.14.19 merge, nullalis hits Reflexion-class reflection storage + partial Voyager-class skill library + ReAct goal pursuit + Anthropic-class cache monitoring. Behind on MemGPT-class agent-curated memory, DSPy-class continuous eval, and CodeAct-class tool synthesis — explicitly parked for v1.16.0+ and V-infinity.

## Next bench-gate trigger

Per AGENTS.md §14.10: the next bench run is paid AFTER:
1. Agent E v1.14.18-A Finding 2 merges (standalone PR)
2. Agent G v1.14.14.1 merges (full 5-finding slice)
3. Agent E continues Findings 1 + 3 in fresh branches and merges those too

THEN bench: LoCoMo 5×10 + V-inf polluted + τ-bench Airline 20-task. This is the run that measures REAL behavioral attribution because v1.14.18-A finally ships:
- Mode unification (effective default 500 iter cap vs silent 25)
- Structured tool descriptions
- Goal-loop reflection between iterations
- Procedural memory actually populating

The expected delta: Cat 3 inference (current 0.33) lifts toward 0.50; τ-bench (current 0.35) lifts toward 0.40-0.45; V-inf polluted (current 0.60, ~0.72 after parser fix) holds or rises with proactive_research recovering via goal-loop reflection escalating to web_search.

## Sign-off

This audit lands BEFORE the next bench run, per §14.10.

Coordinator: Claude (nullalis CTO/PO/repo-designer role this session)
Reviewed-by: Nova (greenlit 2026-05-20)
