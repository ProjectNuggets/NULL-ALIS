---
tags: [prose, prose/docs]
---

# v0.2 Deferred Channels Ledger

Status: active  
Branch: `v0.2-scale-exec-swisswatch`  
Last updated: 2026-03-13

## Purpose

Track channels intentionally deferred from strict rollout so execution stays explicit and auditable.

## Deferred Channels

1. `line`
- rollout stage: `S8 Stage 2B`
- decision: deferred
- owner: runtime/platform
- reason: prioritize currently active webhook channels (`slack`, `lark`, `whatsapp`) and keep rollout momentum with one channel family at a time.
- scope impact: `line` remains on compat path (not strict-enforced) until resumed.
- resume prerequisites:
  - implement/verify Line strict-ingress canary harness
  - run two-track stage evidence (chat-stream stability + Line strict correctness)
  - publish `s8-stage-2b.md` with `GO/HOLD/ROLLBACK`
- rollback behavior while deferred: no action needed; channel not in strict set.

2. `lark`
- rollout stage: `S8 Stage 2C`
- decision: deferred
- owner: runtime/platform
- reason: prioritize currently active webhook channel `whatsapp` next and keep strict rollout focused.
- scope impact: `lark` remains on compat path (not strict-enforced) until resumed.
- resume prerequisites:
  - implement/verify Lark strict-ingress canary harness
  - run two-track stage evidence (chat-stream stability + Lark strict correctness)
  - publish `s8-stage-2c.md` with `GO/HOLD/ROLLBACK`
- rollback behavior while deferred: no action needed; channel not in strict set.

3. `whatsapp`
- rollout stage: `S8 Stage 2D`
- decision: deferred
- owner: runtime/platform
- reason: hold strict channel expansion after `telegram` + `slack` and prioritize scale/capacity hardening work.
- scope impact: `whatsapp` remains on compat path (not strict-enforced) until resumed.
- resume prerequisites:
  - implement/verify WhatsApp strict-ingress canary harness
  - run two-track stage evidence (chat-stream stability + WhatsApp strict correctness)
  - publish `s8-stage-2d.md` with `GO/HOLD/ROLLBACK`
- rollback behavior while deferred: no action needed; channel not in strict set.

## Non-Deferred Next Candidate

1. none in S8 staged strict expansion (currently paused after Stage 2A)

## Decision Log

1. 2026-03-13 — `line` deferred by product/rollout decision; strict rollout proceeds with `lark` next.
2. 2026-03-13 — `lark` deferred by product/rollout decision; strict rollout proceeds with `whatsapp` next.
3. 2026-03-13 — `whatsapp` deferred by product/rollout decision; S8 expansion paused with strict scope fixed to `telegram` + `slack`.
