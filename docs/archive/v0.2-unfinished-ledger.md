# v0.2 Unfinished Ledger

Status: active  
Branch: `v0.2-scale-exec-swisswatch`  
Last updated: 2026-03-13

## 1) Freeze Snapshot (Phase 0 Evidence)

Snapshot source branch:
1. `v0.2-baseline-ledger-utilization`

Snapshot commit:
1. `a33cb29` (`chore(wip): snapshot pre-clean-branch bootstrap (2026-03-12)`)

Pre-snapshot base SHA:
1. `1f0ed2dbf936e799c4ef63c339e96cb49095d06b`

Post-branch clean-state checkpoint:
1. branch `v0.2-scale-exec-swisswatch` created from `a33cb29`
2. `git status --short` empty at branch creation (clean baseline)

Artifact inventory captured under:
1. `docs/reports/canary-2026-03-11/*`
2. `docs/reports/canary-2026-03-12/*`

WIP file intent at snapshot:
1. `src/gateway.zig`
- intent: tenant telegram shared-main configurability and Darwin socket timeout stability guard.
- status: implemented, needs follow-up rollout validation.
2. `scripts/load-burst.py`
- intent: lane strategy support + contention profile fields for canary analysis.
- status: implemented, used by current canary reports.
3. `docs/session-key-policy.md`
- intent: client session lane contract to reduce lock contention.
- status: drafted, now baseline policy document.
4. `docs/v0.2-operational-model-agent-runbook.md`
- intent: cell-routing scale model and rollout gates.
- status: drafted, now active execution runbook.
5. `docs/plan-v02.md`
- intent: link operational runbook into core v0.2 plan.
- status: partially synced; requires final dashboard section (Phase 6).
6. `docs/reports/**`
- intent: preserve canary evidence and historical baseline for comparisons.
- status: committed as immutable run evidence.

## 2) Now-In-Progress Items

## A) Session-Lane Enforcement Path
Owner: runtime/platform agent  
Risk: medium  
Status: completed (server enforcement active)

Remaining:
1. none for v0.2 baseline.

Primary file touch map:
1. `src/gateway.zig`
2. `src/zaki_session.zig`
3. `docs/session-key-policy.md`
4. `docs/openapi-v1.yaml` (if contract wording needs additive clarification)

Acceptance tests:
1. invalid cross-user session key rejected (`400` mismatch).
2. missing/invalid lane behavior follows configured strict mode.
3. legacy fallback behavior preserved only where explicitly intended.

Rollback note:
1. revert strict enforcement flag/path only; keep ownership validation intact.

## B) Gateway/Runtime Scale Path
Owner: platform/sre agent  
Risk: high  
Status: baseline implementation complete (Q2/Q3/Q4 done)

Remaining:
1. run staged canary percentages in real multi-cell environment (10/25/50/75/100).
2. pass latency gates before any rollout promotion claim.

Primary file touch map:
1. `docs/v0.2-operational-model-agent-runbook.md`
2. `docs/multi-instance.md`
3. `docs/reliability-ops-runbook.md`
4. infra manifests outside repo (`deploy/k8s/**`)

Acceptance tests:
1. stable stickiness by `X-Zaki-User-Id`.
2. bounded queue growth under stress.
3. no duplicate ownership execution for same user across nodes.

Rollback note:
1. revert canary traffic split to previous stable percentage.

## C) Canary Evidence Path
Owner: benchmark/sre agent  
Risk: medium  
Status: completed for current posture (decision: controlled rollout GO, scale-claim NO-GO)

Remaining:
1. continue staged canary evidence in multi-cell environment after next tuning cycle.
2. current promotion decision refresh (2026-03-13):
- strict-policy path: hold rollout increase until repeat hard-gate pass
- operator-override path: allow one-step raise with explicit risk acceptance and enhanced monitoring

Primary file touch map:
1. `scripts/load-burst.py`
2. `docs/reports/<date>/*`
3. `docs/reliability-ops-runbook.md`

Acceptance tests:
1. reproducible JSON artifacts for each canary stage.
2. promotion decisions backed by explicit gates.

Rollback note:
1. on gate breach, hold at current rollout percentage and stop promotion.

## 3) Must-Finish Before Scale Claim

1. Rollout increase GO prerequisites (same config/posture):
- two consecutive canary sets (20/50/100) with no hard-gate breach
- 20-user: `p50<20s`, `p95<35s`, `p99<50s`, `error<1%`
- 50-user: `p50<40s`, `p95<70s`, `p99<90s`, `error<1%`
- 100-user: `p50<100s`, `p95<160s`, `p99<220s`, `error<1%`
- isolation evidence: unaffected cohort `p95` degradation `<=15%`
- runtime correctness evidence: `tenant_lock_backend=postgres_lease` + sticky routing verified
2. Public scale-ready claim prerequisites:
- Profile-A repeat pass: `p50<8s`, `p95<20s`, `p99<35s`, `error<1%`
- same pass in multi-cell staging/prod-like setup (not local single-cell only)
- refreshed cost/capacity based on measured `C_cell_month` and `U_paid_cell` at gate-passing latency

Current blocker:
1. strict rollout gates are not yet satisfied on two consecutive 20/50/100 sets at current posture (`docs/reports/2026-03-13-rollout/decision-report.md`).
2. isolation gate instrumentation is available and non-overlapping probe runs were executed, but isolation gate failed at 50/100 cohorts in the latest strict-policy rerun.
3. Profile-A latency pass is not yet satisfied in repeat multi-cell evidence.
4. crash signatures in embedding transport (`OpenAiEmbedding`), state message-load alignment (`zaki_state.dupeResultValue`), and markdown/hygiene path are mitigated on patched SHA; fresh 100-user rerun passed with no new `.ips` crash artifact (`docs/reports/2026-03-13-rollout/final-pass-sequence-report.md`).

## 4) Deferred Items (Explicit)

1. Premium dedicated tier activation.
- deferred until standard tier SLOs and economics are stable.
2. Core async/queue runtime rewrite.
- trigger only when cell model saturates under agreed thresholds.

## 5) Decision Triggers for Deferred Rewrite

Start async/queue architecture program only if any condition persists:
1. p95 SLO breach after quota tuning and cell scale-out.
2. >70% cells at sustained saturation for two weeks.
3. recurring noisy-neighbor incidents despite sticky routing and quotas.

## 6) S8 Staged Strict Rollout Status

Status: in progress (Stage 1 + Stage 2A complete; Stage 2B + Stage 2C + Stage 2D deferred)

Source of truth:
1. `docs/v0.2-s8-manual-rollout-runbook.md`

Completed evidence:
1. Stage 1 (`telegram`) recovery + confirm tracks:
- `docs/reports/2026-03-12-backbone/s8-stage-1.md`
- `docs/reports/2026-03-12-backbone/s8-stage-1-confirm-soak.md`
2. Stage 2A (`slack`) rerun = `GO`:
- `docs/reports/2026-03-12-backbone/s8-stage-2a-rerun.md`

Deferred:
1. Stage 2B (`line`) deferred by explicit decision:
- `docs/v0.2-deferred-channels.md`
2. Stage 2C (`lark`) deferred by explicit decision:
- `docs/v0.2-deferred-channels.md`
3. Stage 2D (`whatsapp`) deferred by explicit decision:
- `docs/v0.2-deferred-channels.md`

Remaining:
1. S8 channel expansion is paused with strict scope fixed to `telegram` + `slack`.
2. continue non-S8 scale hardening and multi-cell canary progression.

## 7) UX / Behavior TODOs (Immediate)

1. STT-first user experience (Telegram voice):
- owner: runtime/product
- priority: P0
- objective: keep text-only TTS optional/deferred and make Telegram voice->STT->reply path operator-visible and user-reliable.
- acceptance:
  - `audio_media.enabled=true` + provider key configured => voice message is transcribed and processed.
  - diagnostics/runtime_info show STT counters increasing (`transcription_attempted/succeeded/failed/skipped_no_transcriber`).
  - failures degrade gracefully (no turn crash; clear operator signal).

2. Group activation policy decision (`activation_mode=mention`):
- owner: product/runtime
- priority: P0
- current behavior: mention mode blocks only when channel metadata explicitly says `mentioned=false`; when mention metadata is missing, message is allowed.
- decision required:
  - keep compatibility default (`allow_if_unknown`) or
  - enable strict mode (`block_if_unknown`) for group channels.
- acceptance:
  - policy is explicit in docs + config.
  - deterministic tests for `mentioned=true/false/null` in group and DM contexts.

3. Discord channel onboarding track (post-Telegram v1):
- owner: product/runtime
- priority: P1
- objective: add user-facing Discord connect/disconnect flow with the same canonical identity + strict ingress correctness model used for Telegram/Slack.
- acceptance:
  - BFF connect/disconnect endpoints + UX flow exist.
  - channel identity extraction contract (`principal/scope/thread`) documented and tested.
  - staged strict rollout evidence produced before promotion.
