You are implementing the v0.2 backbone foundation for tenant+Postgres mode in the `nullalis` repo.

Your job is to execute this as a **high-discipline, sliced implementation program**, not a one-shot patch.

You must follow the plan below exactly.

--------------------------------------------------
MISSION
--------------------------------------------------

Build a single inbound identity backbone so that all tenant+Postgres inbound turns resolve to:

1. one canonical internal user
2. one canonical session key
3. one deterministic canonicalization decision

ALL OF THAT MUST HAPPEN BEFORE DISPATCH.

This is the backbone for:
- tenant isolation
- cross-channel continuity
- sticky routing
- per-cell quotas
- later capacity/cost truth
- runtime truth integrity

This is NOT a generic refactor.
This is NOT a microservices migration.
This is NOT a Kafka project.
This is a correctness-and-scale-foundation project inside the current architecture.

--------------------------------------------------
LOCKED DECISIONS
--------------------------------------------------

These decisions are already made. Do not reopen them.

1. Enforcement mode:
   - staged strict

2. Source of truth for channel identity -> tenant user mapping:
   - DB only
   - config may control rollout policy only, never identity mapping truth

3. Session-key handling:
   - canonicalize before dispatch
   - inbound adapters must not remain authoritative for final session keys

4. Architecture posture:
   - keep modular monolith boundaries
   - do not introduce microservices
   - do not introduce Kafka
   - remain compatible with Kubernetes + containerized deployment

--------------------------------------------------
CURRENT REALITY / GROUND TRUTH
--------------------------------------------------

You are working after the Q-plan completion state.

Current known state:
- Date: 2026-03-12
- Branch: `v0.2-scale-exec-swisswatch`
- Baseline commit: `73f52f3`

Operational posture:
- controlled small-scale rollout: GO
- scale-ready claim: NO-GO
- reliability is acceptable under rollout guardrails
- latency under heavier concurrency is still too high for north-star scale claims

Important truth:
This backbone work is expected to improve correctness, isolation, routing readiness, and observability.
It is NOT expected to dramatically increase raw concurrency by itself.

--------------------------------------------------
CORE IMPLEMENTATION PRINCIPLES
--------------------------------------------------

1. Canonical user identity must be resolved before dispatch.
2. Canonical session key must be built centrally before dispatch.
3. Tenant+Postgres mode must fail closed when mapping is missing on strict channels.
4. All inbound adapters must emit normalized identity metadata, not final authoritative session keys.
5. Runtime truth must become observable:
   - mapped
   - unmapped
   - strict rejected
   - degraded/compat path used
6. No hidden fallback for tenant truth.
7. No mixed-scope mega-patches.
8. No “while here” refactors unless explicitly required by the current slice.

--------------------------------------------------
IMPORTANT DESIGN CORRECTIONS
--------------------------------------------------

You must incorporate these refinements:

A. Canonicalizer hot path
- `inbound_canonicalizer.zig` will be on the hot path.
- Add a small in-memory cache in front of DB lookup:
  - positive TTL cache
  - short negative TTL cache
  - metrics: cache_hit, cache_miss, cache_stale, db_lookup_ms
- DB remains the source of truth.
- Cache is optimization only, never authority.

B. Identity model must separate principal vs scope
Do NOT overload one field to mean both “who the user is” and “where the interaction happened”.

Use separate concepts:
- `principal_key` = external actor identity
- `scope_key` = conversation/group/channel/room scope
- `thread_key` optional

Do not rely only on prefixed `identity_key` strings if that mixes user identity and channel scope.

C. Migration/backfill is required
Before enforcing strict behavior on migrated channels:
- inspect existing session / tenant metadata
- backfill obvious channel bindings
- generate artifact report of:
  - migrated
  - ambiguous
  - missing
- output manual review list

D. Strict-mode user feedback
Strict rejection must not silently disappear if the channel supports replies.
Where possible:
- return explicit “not configured / not onboarded / not authorized yet” style response
- always emit structured reason codes for operators and diagnostics

E. Do NOT turn this into a broad gateway refactor
`gateway.zig` may be large, but do not start a general handler extraction/refactor program in this implementation unless absolutely necessary for the current slice.
Keep changes surgical.

--------------------------------------------------
TARGET END STATE
--------------------------------------------------

By the end of this program:

1. every inbound turn in tenant+Postgres mode reaches dispatch only after canonicalization
2. canonical user mapping is DB-backed and authoritative
3. canonical session key is built centrally
4. strict channels reject unmapped inbound traffic deterministically
5. non-strict channels only use explicit degraded/compat behavior during rollout
6. diagnostics/runtime truth surfaces expose mapping/canonicalization state consistently
7. all supported channels use deterministic principal/scope identity contracts
8. app/API and Telegram share the same authoritative user/session semantics
9. later sticky routing / quotas can safely key on canonical user identity

--------------------------------------------------
DELIVERY RULES
--------------------------------------------------

You must work in slices.
Do not do this in one giant patch.

For every slice:
1. state objective
2. list exact files
3. implement only that scope
4. run tests/gates
5. summarize what changed
6. list evidence
7. stop if scope tries to expand

Do not combine unrelated runtime, infra, and refactor work.

--------------------------------------------------
MANDATORY GATES
--------------------------------------------------

Before and after meaningful slices, run:
- `zig build test --summary all`
- `zig build -Dengines=base,sqlite,postgres`

If a slice breaks gates:
- stop
- isolate cause
- fix or roll back that slice only

Do not continue stacking changes on failing foundations.

--------------------------------------------------
IMPLEMENTATION SLICES
--------------------------------------------------

Execute in this order.

====================
S0 — PREFLIGHT STABILIZATION
====================

Objective:
Freeze current state before backbone work.

Steps:
1. Capture current SHA.
2. Capture dirty file inventory.
3. Record intended inclusion.
4. Run build/test gates.
5. Commit scoped WIP if needed as the baseline for this program.
6. Add a baseline artifact note in docs/reports.

Done when:
- working tree is clean
- baseline is explicit
- gates pass

====================
S0.5 — IDENTITY BINDING BACKFILL / MIGRATION
====================

Objective:
Backfill existing obvious bindings before strict enforcement expands.

Steps:
1. Inspect existing tenant/session metadata and any current Telegram-specific canonical paths.
2. Derive obvious binding candidates.
3. Populate binding records for high-confidence matches only.
4. Produce artifact report by channel:
   - migrated
   - ambiguous
   - missing
5. Produce manual review list for ambiguous items.

Constraints:
- no risky guess-based binding
- ambiguous candidates must not be auto-bound

Done when:
- initial binding coverage is materially improved
- migration artifacts exist
- manual review list exists

====================
S1 — DB IDENTITY MAPPING CORE
====================

Objective:
Create DB-backed authoritative channel identity mapping.

Files:
- `src/zaki_state.zig`

Requirements:
1. Add authoritative table for channel identity bindings.
2. Recommended model includes at least:
   - id
   - user_id
   - channel
   - account_id
   - principal_key
   - scope_key
   - thread_key optional
   - peer_kind
   - peer_id
   - metadata JSONB
   - created_at
   - updated_at
3. Add deterministic uniqueness constraints appropriate for principal identity resolution.
4. Add state methods:
   - upsert binding
   - resolve user by channel identity
   - list bindings
   - delete binding

Tests:
- deterministic upsert behavior
- resolve returns exact user or null
- delete/list behavior
- uniqueness and conflict behavior

Done when:
- schema migrates cleanly
- state tests pass

====================
S2 — SINGLE CANONICALIZER MODULE
====================

Objective:
Introduce one centralized pre-dispatch canonicalizer.

Files:
- new `src/inbound_canonicalizer.zig`
- `src/zaki_session.zig`

Requirements:
1. Define types:
   - `InboundIdentityEnvelope`
   - `CanonicalizationDecision`
   - `CanonicalSessionLane`
2. `InboundIdentityEnvelope` must carry normalized inbound metadata, including:
   - channel
   - account_id
   - principal_key
   - scope_key
   - thread_key optional
   - message/event identifiers where useful
   - optional proposed lane hint
3. Implement `canonicalizeInboundTurn(...)`:
   - resolve user from DB bindings
   - use local TTL cache in front of DB lookup
   - build canonical session key
   - return one of:
     - canonical success
     - explicit degraded/compat decision
     - strict reject
4. Canonical session key shape must remain centralized and deterministic.

Tests:
- lane build correctness
- strict unmapped -> reject
- non-strict unmapped -> explicit degraded decision
- mapped identity -> canonical session always
- cache hit/miss behavior
- cache invalidation behavior on binding mutation

Done when:
- tenant truth no longer depends on parsing arbitrary inbound session keys

====================
S3 — GATEWAY INBOUND WIRING
====================

Objective:
Make gateway/webhook ingress use canonicalization before dispatch.

Files:
- `src/gateway.zig`

Requirements:
1. All relevant webhook handlers construct `InboundIdentityEnvelope`.
2. Call canonicalizer before dispatch.
3. In tenant+Postgres:
   - strict channel + unmapped -> fail closed
   - mapped -> dispatch using canonical session key only
4. Preserve non-tenant behavior unchanged.
5. Stop trusting adapter-generated session keys for tenant authority.
6. Where channel transport supports it, return explicit user-visible “not configured / not connected yet” style response on strict unmapped rejection.
7. Always emit structured rejection reason codes.

Constraints:
- keep changes surgical
- do not start a general gateway decomposition/refactor here

Tests:
- Telegram mapped path canonicalizes
- strict unmapped behavior for supported webhook channels
- non-tenant behavior unchanged
- explicit reason codes emitted consistently

Done when:
- tenant webhook dispatch uses centralized canonical truth only

====================
S4 — DAEMON + POLLING + BUS WIRING
====================

Objective:
Extend the same canonicalization model to daemon, polling, and bus/event paths.

Files:
- `src/daemon.zig`
- `src/channel_loop.zig`
- `src/channel_adapters.zig`

Requirements:
1. Inbound dispatcher uses canonicalizer output.
2. Polling paths use canonicalizer before processing inbound messages.
3. Tenant context is set from canonicalization decision, not arbitrary session-key parsing.
4. Maintain staged strict behavior by channel.

Tests:
- mapped bus/polling path -> canonical key
- unmapped strict -> deterministic reject/drop + metric
- tenant context completeness follows canonical decision

Done when:
- webhook, bus, and polling follow one identity authority model

====================
S5 — CHANNEL IDENTITY CONTRACT
====================

Objective:
Define deterministic per-channel identity extraction without mixing principal identity and conversation scope.

Files:
- new `src/channel_identity_key.zig`
- wiring in `src/gateway.zig`
- wiring in `src/channel_adapters.zig`

Requirements:
1. For each channel, define:
   - principal_key
   - scope_key
   - thread_key if applicable
2. Ensure deterministic extraction from adapter metadata.
3. Ensure uniqueness and semantic correctness.
4. Avoid designs where a group/channel/room ID is mistakenly treated as the user identity.

Examples:
- Telegram:
  - principal_key = sender/from.id
  - scope_key = chat.id
  - thread_key = topic/message_thread_id if present
- Slack:
  - principal_key = user id
  - scope_key = channel id / conversation context
  - thread_key = thread ts if applicable
- WhatsApp:
  - principal_key = sender identity
  - scope_key = group/direct conversation scope
- Email:
  - principal_key = normalized sender address
  - scope_key = mailbox/thread context if needed
- Scheduler/proactive:
  - no external mapping; must already carry internal user identity

Tests:
- deterministic extraction by channel
- no semantic collisions
- correct principal/scope separation
- no cross-user misbinding due to scope confusion

Done when:
- every supported inbound adapter has a deterministic, semantically correct identity contract

====================
S6 — BINDING MANAGEMENT API + DOCS
====================

Objective:
Allow ops/app to manage bindings without config edits.

Files:
- `src/gateway.zig`
- `docs/openapi-v1.yaml`

Requirements:
1. Add binding CRUD endpoints.
2. Validate channel/account/principal/scope/thread fields as applicable.
3. Document request/response schemas and errors.
4. Keep config out of identity mapping values.

Tests:
- create/list/delete binding API behavior
- invalid payload tests
- ownership/authorization error tests

Done when:
- mappings can be provisioned operationally via API

====================
S7 — RUNTIME TRUTH + OPERATOR SURFACES
====================

Objective:
Make mapping/canonicalization state visible and consistent everywhere.

Files:
- `src/tools/runtime_info.zig`
- `src/diagnostics/runtime_truth.zig`
- `src/status.zig`
- `src/doctor.zig`

Requirements:
1. Expose:
   - mapped
   - unmapped
   - strict_rejected
   - degraded/compat_used
   - cache stats
   - mapping source = db
2. Ensure status/runtime_info/doctor/diagnostics agree.
3. Mark unresolved identity on strict channels as actionable operator error.
4. Add “Identity Mapping Health” operator visibility.

Tests:
- parity across surfaces
- strict reject reflected consistently
- runtime truth surfaces do not contradict one another

Done when:
- operators can audit mapping health and strict behavior confidently

====================
S8 — ROLLOUT + CANARY
====================

Objective:
Expand staged strict safely.

Recommended stages:
1. Stage 1: Telegram
2. Stage 2: Slack, Line, Lark, WhatsApp
3. Stage 3: remaining inbound channels

For each stage:
1. enable strictness for that channel set
2. run canary
3. verify:
   - error rate < 1%
   - strict rejects are explained and expected
   - no cross-user leakage
   - no regression in chat stream contract
4. if issues occur, rollback only by removing the channel from strict-channel rollout policy

Also add:
- mapping coverage % by channel
- alert threshold for unexpected strict reject rate
- operator-facing identity mapping health section

Done when:
- supported migrated channels are strict-by-default with evidence

--------------------------------------------------
TEST MATRIX
--------------------------------------------------

Run and maintain these tests throughout:

1. Unit
- canonicalizer decisions
- lane builders
- DB resolver
- cache behavior
- channel identity extraction

2. Integration
- mapped/unmapped webhook behavior
- daemon/bus canonicalization
- polling canonicalization
- binding CRUD API

3. Security / isolation
- cross-user binding misuse rejected
- session ownership checks remain intact
- no tenant context degradation from unmapped inbound paths
- strict reject cannot silently bypass tenant safety

4. Runtime truth
- all operator/runtime surfaces agree on mapping state
- metrics and diagnostics reconcile with request behavior

5. Regression
- `zig build test --summary all`
- `zig build -Dengines=base,sqlite,postgres`

--------------------------------------------------
NON-GOALS
--------------------------------------------------

Do NOT:
- introduce Kafka
- split into microservices
- redesign K8s topology
- rewrite the whole gateway
- change non-tenant behavior unless explicitly required
- mix this with sticky routing, quotas, or capacity work in the same slice

Those may come later, but not in this program.

--------------------------------------------------
SUCCESS CRITERIA
--------------------------------------------------

This program is successful only when all of the following are true:

1. every inbound tenant+Postgres turn is canonicalized before dispatch
2. canonical user mapping is DB-backed and authoritative
3. canonical session keys are built centrally
4. strict channels fail closed on unmapped inbound traffic
5. degraded/compat behavior is explicit, measurable, and temporary
6. app/API and Telegram share one authoritative user/session model
7. runtime truth surfaces expose mapping/canonicalization state consistently
8. later sticky routing and quotas can safely rely on canonical user identity

--------------------------------------------------
WORKING STYLE
--------------------------------------------------

Work slice-by-slice.
After each slice:
1. say what changed
2. show exact files changed
3. show tests run
4. show evidence / artifacts
5. show risks / open points
6. stop before starting the next slice unless the current one is complete

Do not one-shot this.
Do not improvise new architecture.
Do not relax the locked decisions.