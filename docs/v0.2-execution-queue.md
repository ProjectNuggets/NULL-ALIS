# v0.2 Execution Queue (Strict Order)

Branch purpose: execute v0.2 scale hardening from a clean bootstrap state with no decision drift.  
Active branch: `v0.2-scale-exec-swisswatch`  
Last updated: 2026-03-12

## Progress Snapshot

1. `Q1` completed (gateway lane policy enforced; strict-mode optional).
2. `Q2` completed (Postgres-first tenant ownership lease path wired; file-lock fallback retained).
3. `Q3` completed (sticky contract + probe tooling + infra docs).
4. `Q4` completed (explicit overload controls + separate overload metrics).
5. `Q5` completed (canary artifacts produced; decision = conditional rollout GO, scale-claim NO-GO).
6. `Q6` completed (capacity/cost refresh from measured artifacts).
7. S8 manual staged-strict operator runbook published:
- `docs/v0.2-s8-manual-rollout-runbook.md`
8. Active next item: run S8 staged strict promotions per manual runbook.

## Global Rules

1. Execute Q1 → Q6 in order.
2. One queue item = one mergeable branch slice.
3. No mixed feature + refactor + infra in one slice.
4. Mandatory gates per slice:
```bash
zig build test --summary all
zig build -Dengines=base,sqlite,postgres
```
5. Every canary run must produce:
- one JSON artifact
- one short markdown interpretation note

---

## Q1 — Gateway-Enforced `session_key` Lane Policy

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. move from client-trust policy to gateway-enforced lane correctness.

Concrete files:
1. `src/gateway.zig`
2. `src/zaki_session.zig`
3. `docs/session-key-policy.md`
4. `docs/openapi-v1.yaml` (additive clarification only if required)

Implementation steps:
1. define lane-class validator for tenant keys:
- `main`
- `thread:<id>`
- `task:<id>`
- `cron:<id>`
2. enforce ownership + lane-class checks in `/api/v1/chat/stream`.
3. define strict mode behavior for missing `session_key` (explicitly documented fallback policy).
4. preserve backward compatibility path only where explicitly configured.

Tests:
1. valid user-owned lanes accepted.
2. cross-user key rejected.
3. malformed lane key rejected.
4. strict-mode missing `session_key` behavior validated.

Evidence artifacts:
1. unit test outputs.
2. one before/after curl trace for accepted/rejected session keys.

Done criteria:
1. server, not client, is the final lane-policy authority.

Completion notes:
1. tenant lane classes enforced in gateway (`main`, `thread`, `task`, `cron`).
2. cross-user and invalid-lane keys return explicit 400 errors.
3. optional strict mode added: `gateway.require_explicit_chat_stream_session_key`.
4. test gates passed:
```bash
zig build test --summary all
zig build -Dengines=base,sqlite,postgres
```

---

## Q2 — Tenant Ownership Lease Model (Postgres-First Path)

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. define and stage migration away from shared-filesystem ownership lock dependency.

Concrete files:
1. `src/gateway.zig`
2. `src/zaki_state.zig`
3. `docs/multi-instance.md`
4. `docs/reliability-ops-runbook.md`

Implementation steps:
1. design lease table schema and acquisition/renew/release semantics.
2. add dual-path support:
- postgres lease when `state.backend=postgres`
- file-lock fallback only for file mode
3. add diagnostics fields indicating active lease backend.
4. publish migration and rollback procedure.

Tests:
1. lease acquire conflict behavior deterministic.
2. lease expiry and takeover behavior deterministic.
3. no double-ownership under concurrent claim attempts.

Evidence artifacts:
1. conflict test logs.
2. diagnostics snapshots showing lease backend.

Done criteria:
1. Postgres tenant deployments no longer require shared filesystem lock correctness for ownership arbitration.

Completion notes:
1. added `{schema}.tenant_user_leases` and gateway Postgres lease acquire/release path.
2. file lock remains fallback for file-mode runtimes.
3. diagnostics now includes `tenant_lock_backend` (`postgres_lease|file_lock|disabled`).
4. docs updated:
- `docs/multi-instance.md`
- `docs/reliability-ops-runbook.md`
5. test gates passed:
```bash
zig build test --summary all
zig build -Dengines=base,sqlite,postgres
```

---

## Q3 — Cell Sticky Routing Contract + Infra Checklist

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. ensure same user consistently lands on same cell.

Concrete files:
1. `docs/v0.2-operational-model-agent-runbook.md`
2. `docs/multi-instance.md`
3. `deploy/k8s/**` (if in-repo manifests are used)

Implementation steps:
1. define routing key precedence (`X-Zaki-User-Id` then auth user id).
2. define ingress/load-balancer stickiness configuration checklist.
3. add validation probes for stickiness drift.

Tests:
1. repeated user requests land on same cell.
2. failover behavior documented and verified.

Evidence artifacts:
1. routing trace sample.
2. stickiness verification report.

Done criteria:
1. user-to-cell mapping is predictable and observable.

Completion notes:
1. sticky-routing contract documented with precedence in:
- `docs/v0.2-operational-model-agent-runbook.md`
- `deploy/k8s/zaki-bot/README.md`
2. k8s ingress hash routing contract documented (`X-Zaki-User-Id` with Telegram `user_id` fallback).
3. validation probe script added:
- `deploy/k8s/zaki-bot/stickiness-probe.sh`
4. script validation:
```bash
bash -n deploy/k8s/zaki-bot/stickiness-probe.sh
```
5. live-cluster stickiness report generation is queued under Q5 canary stage.

---

## Q4 — Per-Cell Quotas and Backpressure Tuning

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. bound overload impact and remove silent deep queueing.

Concrete files:
1. `src/config_types.zig`
2. `src/config_parse.zig`
3. `src/gateway.zig`
4. `docs/reliability-ops-runbook.md`

Implementation steps:
1. set per-cell queue and worker limits by profile.
2. implement or tighten explicit overload responses (`429/503 + retry hint`).
3. separate interactive pressure from background pressure budget where possible.

Tests:
1. queue cap behavior deterministic.
2. overload response emitted instead of unbounded wait.
3. normal-load latency unaffected.

Evidence artifacts:
1. load test report with queue metrics.
2. error-rate and latency comparison.

Done criteria:
1. degradation is bounded and explicit.

Completion notes:
1. introduced `gateway.overload_retry_after_secs` config (parsed + tested).
2. added explicit overload response header `Retry-After` for both drain and queue-overload `503` paths.
3. split queue overload accounting from drain accounting:
- `overload_rejected_total` (new)
- `drain_rejected_total` (drain-only)
4. metrics/diagnostics updated:
- `nullalis_gateway_overload_rejected_total`
- diagnostics `gateway.overload_rejected_total`
5. docs updated:
- `docs/reliability-ops-runbook.md`
- `docs/v0.2-operational-model-agent-runbook.md`
- `deploy/k8s/zaki-bot/README.md`
6. gates passed:
```bash
zig build test --summary all
zig build -Dengines=base,sqlite,postgres
```

---

## Q5 — Canary Suite and Promotion Gates

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. enforce evidence-based rollout.

Concrete files:
1. `scripts/load-burst.py`
2. `docs/reports/<date>/*`
3. `docs/reliability-ops-runbook.md`

Implementation steps:
1. run 10% → 25% → 50% → 75% → 100% progression.
2. at each stage run:
- multi-user 20
- mixed-real 50
- multi-user 100
3. evaluate against SLO + error + isolation gates.

Tests:
1. reproducibility check (same posture, repeat run).
2. gate pass/fail automation checklist.

Evidence artifacts:
1. per-stage JSON and markdown summary.

Done criteria:
1. each promotion is defensible with artifacted gate pass.

Completion notes:
1. generated canary artifacts under:
- `docs/reports/canary-2026-03-12-q5/canary-20-main.json`
- `docs/reports/canary-2026-03-12-q5/canary-50-mixed.json`
- `docs/reports/canary-2026-03-12-q5/canary-100-main.json`
2. generated summary:
- `docs/reports/canary-2026-03-12-q5/canary-suite-report.md`
3. gate result:
- error-rate gate passed
- profile-A latency gates failed
- profile-B rollout guardrails passed
- decision = conditional rollout GO; scale-claim NO-GO

---

## Q6 — Capacity + Cost Refresh (Measured, no LLM cost)

Status: completed on `v0.2-scale-exec-swisswatch` (2026-03-12)

Objective:
1. publish current infra cost and capacity envelope from measured canaries.

Concrete files:
1. `docs/reports/<date>/capacity-cost-refresh.md`
2. `docs/plan-v02.md`
3. `docs/v0.2-operational-model-agent-runbook.md`

Implementation steps:
1. map measured p95/p99 curves to active-users-per-cell envelopes.
2. compute per-user/month infra cost ranges (excluding LLM).
3. define standard vs premium deployment economics.

Tests:
1. arithmetic reproducibility (source numbers + formulas shown).
2. assumptions explicitly listed and bounded.

Evidence artifacts:
1. final capacity-cost markdown report.

Completion notes:
1. added measured refresh:
- `docs/reports/canary-2026-03-12-q5/capacity-cost-refresh.md`
2. includes:
- measured latency envelope from current canary artifacts
- active-users-per-cell planning envelope
- per-user/month infra-cost sensitivity table (excluding LLM cost)

Done criteria:
1. product and infra decisions use measured economics, not intuition.
