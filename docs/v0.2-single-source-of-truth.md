# v0.2 Single Source Of Truth (Ops + Scale)

Status: canonical operational truth  
Date: 2026-03-13  
Active branch: `v0.2-scale-exec-swisswatch`  
Branch execution log: `docs/branches/v0.2-scale-exec-swisswatch.md`

## Documentation Contract

This is the only canonical ops/status document for v0.2.

1. Runtime truth and deployment decisions live here.
2. Each active branch gets one execution log under `docs/branches/`.
3. Everything under `docs/reports/` is evidence, not policy.
4. Older plans/ledgers are historical context only.

## Current Decision State

1. Lab/runtime patch line: `GO`.
2. Strict-policy rollout increase: `HOLD` until hard-gate sequence is re-passed.
3. Operator override: allowed for one-step raise with explicit risk acceptance and immediate rollback guardrails.
4. Public scale-ready claim: `NO-GO`.

Primary evidence:
1. `docs/reports/2026-03-13-rollout/final-pass-sequence-report.md`
2. `docs/reports/2026-03-13-rollout/final-100-fresh-after-markdown-fix.json`
3. `docs/reports/2026-03-13-rollout/decision-report.md`

## What Is Implemented (Code Truth)

1. Gateway-enforced session lane policy (`main|thread|task|cron`) + ownership checks.
2. Postgres-first tenant ownership lease path with file-lock fallback.
3. Identity canonicalization path with staged strict support.
4. Backpressure and overload signaling (`Retry-After`, explicit overload/drain metrics).
5. Tool dispatcher wiring + runtime truth surfaces.
6. Stability fixes for load-history, Postgres message-load alignment, markdown/hygiene crash paths.
7. V1 shell/git sandbox wiring (opt-in via `security.sandbox.*`, fail-closed when enabled and unavailable).
8. Inert runtime knob activation (STT-first):
- STT telemetry + diagnostics/runtime surfaces
- `agent.compact_context`
- `session_ttl_secs`
- `activation_mode`
- `send_mode`
- `queue_*`
- `tts_*` (runtime payload prep path)
9. QA/QS hotfix alignment completed:
- TTL race removed via in-lock session recycle (no destructive TTL removal in `getOrCreate`)
- `queue_mode=off` bypasses queue cap/drop/debounce logic
- `queue_drop=summarize` now injects a deterministic synthetic summary into the next admitted turn
- `agent.*` queue/ttl/activation/send/tts knobs now wired from config + runtime slash overrides
- `tts_audio=true` no longer mutates assistant text output

Primary code areas:
1. `src/gateway.zig`
2. `src/zaki_state.zig`
3. `src/inbound_canonicalizer.zig`
4. `src/memory/engines/markdown.zig`
5. `src/memory/lifecycle/hygiene.zig`
6. `src/agent/root.zig`

## Load Profiles And Gates

Profile A (public scale-claim gate):
1. `p50 < 8s`
2. `p95 < 20s`
3. `p99 < 35s`
4. error `< 1%`

Profile B (rollout guardrail):
1. 20 users: `p50 < 20s`, `p95 < 35s`, `p99 < 50s`, error `< 1%`
2. 50 users: `p50 < 40s`, `p95 < 70s`, `p99 < 90s`, error `< 1%`
3. 100 users: `p50 < 100s`, `p95 < 160s`, `p99 < 220s`, error `< 1%`

Isolation gate:
1. unaffected cohort `p95` degradation `<= 15%`

## Capacity Snapshot (Current Envelope)

1. strict interactive envelope (`p95 < 20s`): ~`10–14` active heavy users per cell
2. relaxed envelope (`p95 < 35s`): ~`18–24` active heavy users per cell
3. planning approximation:
- 1 pod: `15–25` active turns
- 6 pods: `90–150`
- 12 pods: `180–300`

## Open Construction Sites

1. Multi-cell staged canary evidence (10/25/50/75/100).
2. Sticky routing proof in cluster.
3. Postgres lease backend proof in cluster.
4. Isolation/noisy-neighbor gate proof in cluster.
5. Monetization enforcement slice (entitlements + metering).
6. Multi-cell validation for the newly wired queue/ttl knobs under real cluster routing.

Deferred by policy:
1. Strict expansion for `line`, `lark`, `whatsapp` (compat path retained).
2. Async/queue core rewrite (only if saturation persists after cell tuning).

## Immediate Next Sequence

1. Keep current lab state as baseline.
2. Move to deployment/monetization execution slices, not more local-document churn.
3. Use branch log for step-by-step execution updates and decisions.
