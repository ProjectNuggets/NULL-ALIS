---
tags: [prose, prose/docs]
---

# V1.8 Code Review — pre-compaction sweep 2026-05-05

**Author:** Claude (self-review with adversarial attention).
**Scope:** all source-touching commits between `ad71bed` (Phase 6) and `2da6f40` (V1.8-5). 4 commits, 7 files, 402 lines diff. `zig build test` exit 0. Live smoke verified per commit.

**Method:** read each diff with "what could be wrong" mindset, not "what did I intend." Trace ownership, lifetimes, ordering, edge cases. Categorized findings: 🔴 blocker / 🟡 medium / 🔵 cosmetic.

**Verdict:** clear to ship V1.8-4 next. No blockers. Three medium-low findings + a few cosmetics, all with documented "acceptable" reasons or deferred to V1.9.

---

## V1.8-1 — judge wire + contradiction-discard fix

**Files:** `src/gateway.zig` (+11 / -7), `src/agent/extraction_persist.zig` (+25 / -16)

### Findings

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 1 | 🔵 | `runtime.config.default_model` is shallow-copied from `base_config` and stored on `MemoryStoreTool.judge_model_name`. Slice points into base_config string memory. Lifetime: gateway state outlives tools. ✓ Safe. | Documented |
| 2 | 🟡 | `applyContradictions` now runs even when `is_duplicate=true`. Verified semantics: judge already determined dup vs contradiction PRE-call; no re-evaluation needed. Old fact closes; new write skipped because dup. Final state: one active per (subject, predicate). | Working as designed |
| 3 | 🔵 | `provider_i` defined at gateway.zig:1316, used at line 1567 — same `pub fn init` scope, accessible. ✓ | Working as designed |

### Smoke evidence
- preference_changes corpus: F1 0.571 → 1.000 (clean run); 0.667 (cross-corpus contaminated run)
- Live log: `edge_resolution.contradiction` + `extraction.contradictions_applied count=N` both present (was missing pre-fix)
- `event_type='edge_closed'` count: 0 → 2-7 per corpus run

---

## V1.8-2 — embedding coverage on persistExtracted

**Files:** `src/agent/extraction_persist.zig` (+18), `src/agent/compaction.zig` (+1), `src/agent/commands.zig` (+2), `src/tools/memory_store.zig` (+1), `src/zaki_state.zig` (+1)

### Findings

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 4 | 🔵 | `m.text` lifetime — input slice owned by caller, lives for the loop iteration. `syncVectorAfterStore` is sync (no async retention beyond the call). ✓ | Working as designed |
| 5 | 🔵 | `mem_rt` parameter ordering: appended LAST after `coref_ctx`. Doesn't break callsites that omit it (none did — all 4 explicit callsites updated). | Working as designed |
| 6 | 🔵 | Vector sync position: BEFORE `extraction.persisted` log + counter increment. If sync fails silently, memory is still counted as "written" — correct (memory row IS canonical). | Working as designed |

### Smoke evidence
- 10/10 most recent `extracted_*` memories in PG have `has_vec=t` (was 0/10 pre-V1.8-2)
- embedding_coverage_pct (recent-scope): 53% → 79%

---

## V1.8-3 — forget cascade to edges

**Files:** `src/zaki_state.zig` (+131 / -11)

The biggest single change. Most carefully reviewed.

### Findings

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 7 | 🟡 | **Memory leak on rare OOM path.** In the cascade-row-capture loop (zaki_state.zig:~4243), if `try closed_edges.append(...)` fails AFTER allocating src/tgt/pred for the current iteration, those allocations leak (the struct fields are bound to the same `e` that was supposed to enter the list). The `defer` only frees items already in the list. | **Acceptable**: only fires on OOM during ArrayList grow within the cascade loop. Path is proportional to number of cascaded edges (typically 1-5). System-allocator failure here means the system is already in trouble. Low-priority follow-up; could `errdefer` the trio inside the iteration. |
| 8 | 🔵 | Order: cascade UPDATE BEFORE memories DELETE (within the same transaction). Postgres MVCC keeps other transactions from seeing partial state until COMMIT. ✓ | Working as designed |
| 9 | 🔵 | `errdefer ROLLBACK` becomes inactive after `try self.exec("COMMIT")` succeeds. Post-COMMIT event emission failures are best-effort `catch`-and-log, do NOT trigger rollback. ✓ | Working as designed |
| 10 | 🔵 | Per-row `dupeResultValue` failure handling: each subsequent failure manually frees prior allocations of THIS iteration via early-return-with-cleanup pattern. Tedious but correct. Could be cleaner with arena, but works. | Acceptable |
| 11 | 🔵 | `now_ts` (i64) used for valid_to / invalid_at / expired_at — same epoch-seconds convention as setMemoryInvalidation. Consistent. ✓ | Working as designed |

### Smoke evidence
- mem_exists: 1 → 0
- edges_active: 1 → 0
- edges_closed: 0 → 1
- Log: `forget.cascade user=7777 key=extracted_041535a9e15f9b8e edges_closed=1 memory_deleted=true`
- memory_events row with full payload `{op, predicate, confidence, source_key, target_key}`
- BONUS: gateway boot's `syncFromMarkdown` triggered backlog cascades on 6 prior orphan tombstones

---

## V1.8-5 — JSON-tail prompt + Pass C archive guarantee

**Files:** `src/agent/compaction.zig` (+19 / -1), `src/memory/lifecycle/summarizer.zig` (+12 / -8)

### Findings

| # | Severity | Finding | Disposition |
|---|---|---|---|
| 12 | 🟡 | **Pass C log fires even when no compaction will happen.** When non-system message count ≤4, the `cap_keep` floor of 4 means nothing gets compacted. Log "pass=C firing" misleads. Cosmetic but observability-relevant. | **Defer to V1.9**: move the log AFTER `compactHistoryKeepingRecent` and condition on its return value (true). One-line fix; not blocking. |
| 13 | 🔵 | `cap_keep` arithmetic: `@max(@min(half, 20), 4)`. Edge cases traced (history len 0, 1, 5, 17, 25, 100) — all behave correctly. The intermediate `@max(history.items.len, 1) -| 1` correctly handles len=0 via saturating subtraction. | Working as designed |
| 14 | 🔵 | `\n` in summarizer prompt is the literal two-char sequence (Zig source `\\n` → output `\n`). LLM must interpret as newline. Existing parser handles both interpretations; risk low. | Acceptable |

### Smoke evidence
- pre_passC_count: 0
- post_passC_count: 2 (after 6 long-prompt turns with token_limit=8000)
- Log: `compaction.auto: compacted before=34051 after=31926 saved=6%`

---

## Cross-cutting: things checked + clean

- **`zig build test` exit 0** — all existing tests pass. No regression on shipped functionality.
- **`zig build -Doptimize=ReleaseFast -Dengines=all`** — clean build. Postgres engine compiled in. Binary 9.88MB.
- **Live gateway smoke** — V1.8-0 fix held through 5+ cold-restart cycles + heavy V1.8-7 audit + 6+ V1.8-5 verification turns. No panics. Zero `error.PostgresNotEnabled` after `-Dengines=all` build.
- **Cross-commit eval F1** held within ±0.05 across V1.8-1/2/3/5 runs (cross-corpus contamination is the dominant variance, not regressions).

## Things deliberately NOT done (acceptable scope cap)

| Item | Reason | Tracked |
|---|---|---|
| Per-corpus eval test users | Single-tenant contamination noise-floors V1.8-2/3 gains. Documented in eval suite README. | V1.8-6 v2 / V1.9 |
| Pass C log-after-success rewording | Cosmetic observability. Not blocking ship. | V1.9 |
| `errdefer` cleanup for the cascade-loop trio leak | Only fires on OOM during ArrayList append. Low impact. | V1.9 |
| pre-V1.8 backlog of unembedded memories | V1.8-2 only fixes the forward path. Backfill is one-time SQL. | Out of sprint |
| primaryModelName() accessor on RuntimeProviderBundle | Used `runtime.config.default_model` directly. Bundle accessor is nice-to-have. | V1.9 |

## Pre-compaction sanity confirmation

Working state for the next-me to inherit:

- Gateway PID alive, V1.8-0 verified across cycles
- Config restored to pre-audit state (no `token_limit` override)
- `agent.token_limit_explicit` triggers correctly when token_limit is set in config
- 152 commits unpushed; no uncommitted changes in `src/`
- `.audit/v1.8/evals/` baseline locked + reproducible
- All Phase 0-7 docs current

**Ready for V1.8-4** with clean cognitive room. The next module (`src/agent/cheap_extraction.zig`) is ~205 LOC of NEW code — biggest single piece left. Worth the fresh attention.

---

_2026-05-05 pre-compaction. Authored by the same Claude instance that shipped the work. Kept binding [[feedback_next_generation]]._
