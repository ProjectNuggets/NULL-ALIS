---
tags: [prose, prose/docs]
---

# V1.8 Sprint Lock — Phase 5 closed 2026-05-04

**Authored:** 2026-05-04 post-Phase-4. Locks the V1.8 sprint per Swiss-watch discipline.

**Inputs:** `docs/v1.8-memory-pyramid-design.md` (Phase 3, design), `docs/v1.8-memory-gap-matrix.md` (Phase 4, code reality), `.audit/v1.8/runs/kimi-pass-c-20260504-215527/FINDINGS.md` (V1.8-7 attempt blocked by gateway crash).

**Status:** SCOPE LOCKED. Phase 6 plans each commit, Phase 7 builds.

---

## Lock summary

| Field | Value |
|---|---|
| Items must-ship | **9** (V1.8-0 added P0 stability fix) |
| Items queued | **2** (V1.8-8, V1.8-10 — not dropped, post-V1.8) |
| Items deferred | **3** (V1.8-11/12/13 — could-ship from design doc, deferred per Phase 4) |
| Estimated LOC | ~800-830 (V1.8-0 + ~767 from Phase 4) |
| Estimated wall time | ~7-9 days focused build |
| Branch | `main` |
| Baseline sha | `7678817` (Phase 4 gap matrix) |
| Ship gate | adversarial eval suite ≥80% F1 + 7 audit-verifiable criteria |
| Driver | Claude builds; Nova reviews + greenlights commits |
| Locked principle | [[feedback_next_generation]]: every commit asks "what would the next generation build, knowing what we know in 2026?" |

---

## Locked must-ship list (build order)

| # | Item | LOC est. | Files | Blocks |
|---|---|---|---|---|
| **V1.8-0** | **NEW P0** — fix gateway boot-time crash in `getTenantRuntime` | 5-30 | gateway.zig (+1 file likely TenantRuntime ctor) | EVERYTHING |
| V1.8-7 | Re-run Kimi K2.5 audit with `token_limit=8000` | 0 (config + ops) | none in src/; new run dir + audit appendix | unblocks V1.8-5 scope confirmation |
| V1.8-6 | Adversarial eval suite as CI ship gate | ~300 | new `.audit/v1.8/evals/` | ship gate (must be in place to measure subsequent commits) |
| V1.8-1 | Wire judge_provider in gateway | ~12 | gateway.zig | enables P3 RECONCILE |
| V1.8-2 | Embedding coverage invariant on persistExtracted | ~30 | extraction_persist.zig + memory/root.zig | enables vector retrieval correctness |
| V1.8-3 | Forget cascade to edges | ~25 | zaki_state.zig | closes G-NEW orphan-edges |
| V1.8-5 | Tighten JSON-tail prompts | ~30 | summarizer.zig + compaction.zig | LLM compliance baseline measured BEFORE V1.8-4 |
| V1.8-4 | Deterministic cheap pass `cheap_extraction.zig` | ~205 | new module + agent/root.zig call site | density floor under unreliable LLM |
| V1.8-9 | Identity profile materialization (S8) | ~165 | new module + memory_loader.zig | first-class identity surface |
| **Total** | | **~770-800** | **9 files touched + 4 new modules + eval suite** | |

---

## Why V1.8-0 jumped to position 1

**Discovery context:** V1.8-7 audit attempted on Kimi K2.5 with `agent.token_limit=8000` config override. Gateway crashed on the first chat request. Reproduced 2/2 with both config variants. Same panic site:

```
gateway.zig:1999  TenantRuntime.init(...)
  → state.tenant_runtimes.put(...)
  → std.HashMap.grow → putAssumeCapacityNoClobberContext
  → assert(!self.containsContext(key, ctx)) → FAIL
```

**Why it must lead:** every other V1.8 item requires a stable gateway to validate (the eval suite drives chat requests; audits drive chat requests; even manual smoke testing requires it). Production deploy is also blocked: no restart-safe upgrade path. Ergo this is THE blocker.

**Triage plan:** see `.audit/v1.8/runs/kimi-pass-c-20260504-215527/FINDINGS.md` Recommendation section.

**Sequencing logic:** V1.8-0 → V1.8-7 (proves audit unblocked) → V1.8-6 (eval baseline) → fixes ranked from least risky to most risky.

---

## Parking-lot decisions resolved (per Phase 4 §7)

Each was a real choice; defaults locked here. Phase 6 may revisit if a planner check finds a contradiction.

| # | Question | DECISION | Rationale |
|---|---|---|---|
| **PL-1** | V1.8-3: hard-DELETE memories vs soft-close (bi-temporal) | **DELETE on S2 (memories), bi-temporal close on S5 (edges)** | Preserves current S2 semantics (matches `forget` user intent); makes S5 follow standard close-out pattern (matches supersession); cheap to revisit if eval suite finds an issue. |
| **PL-2** | V1.8-4: regex scope — narrow or broad? | **Start narrow: 5 declarative shapes** ("I am X", "I work at X", "I prefer X over Y", "my name is X", "X = Y" key=value) + 1 entity shape (capitalized phrases ≥2 chars, dedup by lowercase) + 1 date shape (`\d{4}-\d{2}-\d{2}` and "today\|yesterday\|tomorrow") | Lower over-match risk; eval suite evidence drives expansion. |
| **PL-3** | V1.8-4: confidence value for deterministic | **0.6** | Mid-range — retrieval can downweight; judge=on (V1.8-1) reconciles against existing higher-confidence facts. |
| **PL-4** | V1.8-5: Pass C prompt rewrite or targeted swap? | **Targeted swap** at both `summarizer.zig:184-191` and `compaction.zig:887` — change "optionally append" → "MUST append (use `[]` if no triples)" + remove the "If you can't form a clean triple, omit" line | Preserves all working behavior; minimum surface for regression risk. |
| **PL-5** | V1.8-6: where does eval run? | **Local-first**: shell-script `./run_eval.sh` invocable manually + as pre-commit hook. CI integration deferred to V1.9 (no GitHub Actions today). | Matches current dev velocity; eval is the gate even if not automated yet. |
| **PL-6** | V1.8-9: identity profile structure — string or JSON? | **Structured JSON memory block** (queryable + diffable) serialized to a string for prompt injection. Format: `{ "name": "...", "roles": [...], "preferences": [...], "current_focus": [...], "stakeholders": [...] }` | Future-proof: agent can edit fields atomically (Letta pattern); diffs are line-meaningful in markdown export. |

---

## Locked ship criteria (re-anchored from design + gap matrix)

V1.8 ships when ALL of:

1. ✅ Gateway survives 100 sequential cold-restart-then-chat cycles without panic (V1.8-0 verification)
2. ✅ V1.8-7 audit on Kimi K2.5 confirms Pass C fires (or documented why it doesn't — proceed without)
3. ✅ V1.8-6 eval suite green ≥80% F1 across all 5 corpora
4. ✅ memory_doctor reports 100% S3 vector coverage on user 7777
5. ✅ T2→T4 supersession test (eval) closes prior edge — judge=on verified
6. ✅ T11 cross-session recall finds latest preference via S3 vector retrieval, not S9 markdown fallback
7. ✅ T12 forget cascades — S5 edges archived alongside S2 source DELETE
8. ✅ T1 identity prompt writes ≥1 S5 edge from prose (deterministic floor catches it)

V1.7-resume gate (when V1.8 ships):
- Push 143+ V1.7+V1.8 commits to origin
- Tag `v1.7.0` then immediately `v1.8.0`

---

## Estimated effort breakdown

| Item | Coding | Testing | Total |
|---|---|---|---|
| V1.8-0 (triage + fix) | 2-4h | 1h (100-restart smoke loop) | 3-5h |
| V1.8-7 (audit re-run) | 0 | 1.5h | 1.5h |
| V1.8-6 (eval suite) | 1d | included | 1d |
| V1.8-1 | 30min | 30min | 1h |
| V1.8-2 | 2h | 1h | 3h |
| V1.8-3 | 2h | 1h | 3h |
| V1.8-5 | 1h | 2h (LLM compliance measurement) | 3h |
| V1.8-4 | 1d | 4h | 1.5d |
| V1.8-9 | 1d | 4h | 1.5d |
| Integration test pass | — | 4h | 4h |
| **Total** | | | **~7-9 days** |

---

## Atomic commit policy (Phase 7)

Each item lands as ONE atomic commit on `main` matching this commit-message shape:

```
feat(v1.8): V1.8-N — <imperative summary>

<body: what changed, why, smoke test summary>

Closes G-X. Eval F1 delta: <X.XX>.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

For V1.8-0 (a fix not a feature):

```
fix(v1.8): V1.8-0 — getTenantRuntime panic on fresh-boot first-request
```

Each commit MUST:
- Compile clean (`zig build -Doptimize=ReleaseFast`)
- Pass existing tests (`zig build test`)
- Update touched files in `internals/P*.md` per [[feedback_update_internals_on_touch]]
- Run target evaluation (eval suite for V1.8-1..9; smoke loop for V1.8-0)
- NOT regress F1 on other corpora (>5% drop blocks merge per design doc)

---

## Out of scope (re-stated explicitly to avoid drift)

- Pass C threshold re-tuning (deferred until V1.8-7 confirms what's reachable)
- Mem0-style ADD/UPDATE/DELETE/NOOP LLM dispatch (research warned against without adversarial eval)
- Letta agent-callable memory-edit tools (V1.9)
- Multi-resolution communities (premature)
- Code-plane parallel (V2)
- ASSUME INTERRUPTION framing (V1.8-8 — queued)
- Daily summary aggregation (V1.8-10 — queued, needs scheduler infra)
- Sem/epi/proc taxonomy + sleep-time worker promotion + FE memory UX recipe (V1.8-11/12/13)

---

## Open Phase 6 prep

When Phase 6 opens (per-commit Swiss-watch planning), each must-ship gets a commit-plan covering:

1. **Decisions** — anything not pre-decided here (PL-1..6 cover the cross-cutting ones)
2. **Reuse audit** — what existing functions/types this leans on; no duplicate scaffolding
3. **Tests** — unit + integration + eval (which corpus, which expected behavior)
4. **Risks** — what could regress, mitigation
5. **Rollback plan** — single-commit revert path

Phase 6 produces one `.planning/v1.8-N-plan.md` file per item OR a single `docs/v1.8-build-plans.md` consolidating all 9. Decision: **single consolidated file** (matches current docs cadence).

---

## State at lock

- Branch `main` at `7678817`
- 143 commits unpushed
- Gateway PID 26464 currently alive but crashes on next un-lease-blocked chat request (see FINDINGS.md)
- Config restored to pre-V1.8-7-attempt state (no `token_limit` override)
- All Phase 0-4 docs shipped + this Phase 5 lock

---

_Phase 5 closed 2026-05-04. Phase 6 opens with V1.8-0 as the first commit-plan target. Locked binding [[feedback_next_generation]] governs every commit._
